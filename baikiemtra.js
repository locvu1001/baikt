var express = require('express'):
var app = express(():
app.get('/contact'.funtion (req. res){
	res.send('Hello node'):
});
app.listen(3000. funtion (){
    console.log('Example app listening on port 3000!');
});